"use client";
import Button from "@/components/ui/button";

const Actions: React.FC = () => (
    <div className="flex gap-4">
      <Button color="primary" onClick={() => {}} size="md">Login</Button>
      <Button onClick={() => {}} size="md">Sign up</Button>
    </div>
  );

export default Actions;
